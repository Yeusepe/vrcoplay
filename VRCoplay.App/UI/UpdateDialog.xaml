<!-- Copyright (c) 2026 YUCP Studio. SPDX-License-Identifier: GPL-3.0-or-later. -->
<ContentDialog x:Class="VRCoplay.UpdateDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Style="{StaticResource DefaultContentDialogStyle}" DefaultButton="Primary" PrimaryButtonStyle="{StaticResource AccentButtonStyle}" CloseButtonText="Done" PrimaryButtonClick="Primary_Click" FontFamily="{StaticResource ContentControlThemeFontFamily}" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
  <ContentDialog.Resources>
    <x:Double x:Key="ContentDialogMaxWidth">480</x:Double>
    <Thickness x:Key="ContentDialogTitleMargin">0,8,0,16</Thickness>
    <Thickness x:Key="ContentDialogSeparatorThickness">0</Thickness>
    <x:Double x:Key="ContentDialogButtonSpacing">0</x:Double>
    <CornerRadius x:Key="ControlCornerRadius">12</CornerRadius>
    <CornerRadius x:Key="OverlayCornerRadius">24</CornerRadius>
    <StaticResource x:Key="ContentDialogTopOverlay" ResourceKey="SolidBackgroundFillColorBaseBrush" />
  </ContentDialog.Resources>
  <ContentDialog.CloseButtonStyle>
    <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
    </Style>
  </ContentDialog.CloseButtonStyle>
  <ContentDialog.Title>
    <StackPanel Spacing="20">
      <Grid>
        <FontIcon x:Name="UpdateIcon" Glyph="" FontSize="40" Height="48" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" AutomationProperties.AccessibilityView="Raw" />
        <Button x:Name="UpdateOptionsButton" Style="{StaticResource SubtleButtonStyle}" Width="44" Height="44" HorizontalAlignment="Right" VerticalAlignment="Top" AutomationProperties.Name="Update options" ToolTipService.ToolTip="Update options">
          <SymbolIcon Symbol="More" />
          <Button.Flyout>
            <MenuFlyout x:Name="UpdateOptionsMenu">
              <MenuFlyoutItem x:Name="CheckAgainItem" Text="Check for updates" Click="CheckAgain_Click" />
              <MenuFlyoutItem x:Name="SkipVersionItem" Text="Skip this version" Click="SkipVersion_Click" />
              <ToggleMenuFlyoutItem x:Name="AutomaticPromptsItem" Text="Notify me about updates" Click="AutomaticPrompts_Click" />
            </MenuFlyout>
          </Button.Flyout>
        </Button>
      </Grid>
      <TextBlock x:Name="UpdateHeading" Text="Checking for updates" Style="{StaticResource PageTitle}" FontSize="28" TextAlignment="Center" AutomationProperties.HeadingLevel="Level1" AutomationProperties.LiveSetting="Polite" />
    </StackPanel>
  </ContentDialog.Title>
  <ScrollViewer x:Name="UpdateScroll" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
    <StackPanel Spacing="16">
      <TextBlock x:Name="VersionText" Style="{StaticResource QuietText}" TextAlignment="Center" />
      <TextBlock x:Name="UpdateDescription" Style="{StaticResource QuietText}" FontSize="16" TextAlignment="Center" />
      <ProgressBar x:Name="UpdateProgress" IsIndeterminate="True" Visibility="Collapsed" AutomationProperties.Name="Checking for updates" />
      <TextBlock x:Name="UpdateError" Style="{StaticResource QuietText}" TextAlignment="Center" Visibility="Collapsed" Foreground="{ThemeResource SystemFillColorCriticalBrush}" AutomationProperties.LiveSetting="Polite" />
      <Expander x:Name="NotesExpander" Header="What’s new" IsExpanded="False" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
        <TextBlock x:Name="NotesText" TextWrapping="Wrap" IsTextSelectionEnabled="True" FontSize="14" />
      </Expander>
    </StackPanel>
  </ScrollViewer>
</ContentDialog>
